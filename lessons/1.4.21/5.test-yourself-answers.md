# Answers: TaskScheduler.FromCurrentSynchronizationContext

## Scoring

≥80% → `1.4.22`

---

## Answers

### 1. [Recall]
**Answer:** **Queue delegate** для выполнения на **associated thread/context** (async queue).

---

### 2. [Recall]
**Answer:** **`null`** (typically) — no capture.

---

### 3. [Understanding]
**Answer:** **UI thread** (same SynchronizationContext).

---

### 4. [Understanding]
**Answer:** **Thread pool thread** (any available) — no SyncContext capture.

---

### 5. [Understanding]
**Answer:** Не capture UI/legacy context → **avoid deadlocks** when caller blocks + safe for ASP.NET/UI callers.

---

### 6. [Application]
**Answer:** UI thread **blocked** on `.Result`; **`GetDataAsync`** continuation **posts to UI** → circular wait → **deadlock**.

---

### 7. [Application]
**Answer:** **`async void` handler** (event) with **`await GetDataAsync()`** (and try/catch).

---

### 8. [Analysis]
**Answer:** **`async/await`** — `FromCurrentSynchronizationContext` + **`StartNew`** legacy (**`1.4.24`**).

---

### 9. [Analysis]
**Answer:** **Нет** — caller still **blocks UI thread** waiting; library false doesn't fix **`.Result`**.

---

### 10. [Recall]
**Answer:** **No** — **`Task.Run`** schedules to **thread pool**.

---

### 11. [Understanding]
**Answer:** **System.Web** had **`HttpContext`** SyncContext; **ASP.NET Core** intentionally **null** — scalability.

---

### 12. [Application]
**Answer:** **Да** — circuit SyncContext; UI-like rules within Blazor dispatch.

---

### 13. [Understanding]
**Answer:** **UI event handlers** (and similar top-level callbacks) with exception handling — **`1.4.13`**.

---

### 14. [Analysis]
**Answer:** **No** — request thread not guaranteed after await; use **`HttpContextAccessor`**, not thread id.

---

### 15. [Application]
**Answer:** **After `await Task.Run`** — back on **UI thread** (if awaited on UI without ConfigureAwait(false)); safe to update **`Label.Text`**.

---

## If you failed

Re-read: UI deadlock diagram, ASP.NET null context, ConfigureAwait library rule.

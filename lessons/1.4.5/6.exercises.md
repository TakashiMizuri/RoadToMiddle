# Exercises: TaskCompletionSource

## E1. Wrap Timer callback as awaitable Task.
## M1. Two callbacks race — only first completes TCS; log second TrySet false.
## H1. Legacy event-based download wrapper with cancel token + timeout.

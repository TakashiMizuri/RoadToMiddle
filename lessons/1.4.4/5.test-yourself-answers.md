# Answers: Task.Run

### 1. Queue CPU delegate to thread pool; return Task for await.
### 2. SaveChanges sync blocks pool worker entire DB wait — fake async; use SaveChangesAsync.
### 3. await Task.Run(() => BCrypt...) with optional global throttle — not async void.
### 4. ReadAllTextAsync — true async I/O.
### 5. Pool queue explosion, starvation, p99 latency, CPU 100%.
### 6. Task.Run — safer defaults; StartNew legacy pitfalls.
### 7. No — overhead exceeds benefit; sync inline.
### 8. Misleading — HttpGetAsync already async; Task.Run adds extra thread hop without need; call await HttpGetAsync directly on request async method.

# Answers: LINQ SelectMany

### 1. **SelectMany** flat **`Line`**; **Select** → **`IEnumerable<IEnumerable<Line>>`**.

### 2. Desugars to **`SelectMany`** — concat all lines.

### 3. **`posts.SelectMany(p => p.Tags, (p,t) => new { p.Id, t })`**.

### 4. **`SelectMany(x => x.g.DefaultIfEmpty(), ...)`**.

### 5. **`ArgumentNullException`**.

### 6. **6** elements.

### 7. **Cross product** — each **a** with every **b** in **bs**.

### 8. **0** rows from that outer.

### 9. **`matrix.SelectMany(row => row)`**.

### 10. When enumeration reaches that outer element's **`MoveNext`**.

### 11. Extra allocation/step; same semantics, worse readability.

### 12. **`SelectMany(o => o.Coupon != null ? [o.Coupon] : Empty<Coupon>())`** or left join.

---

## If you failed

Flatten vs Select; left join recipe; null guard.

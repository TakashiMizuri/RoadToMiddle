# Answers: ArrayPool

### 1. **Length >= minimumLength** (often larger bucket).
### 2. Zero sensitive stale data for next renter.
### 3. Pool starvation, extra GC allocations, memory growth.
### 4. **No** — use-after-return if field still used.
### 5. **`true`** (or don't pool secrets).
### 6. Often **stackalloc** if fixed 512 and method-scoped; pool if many/larger.
### 7. **Undefined/corruption** — don't double Return.
### 8. **Shared** global; **Create** isolated custom limits.
### 9. **try/finally** Return.
### 10. **Wrong** — only Return **rented** arrays to pool.

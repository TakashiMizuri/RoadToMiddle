# Self-Test: Exception handling in async

> ⛔ Не открывай `5.test-yourself-answers.md` до завершения.
> Время: ~15 мин · Проходной балл: **80%**

---

## Questions

### 1. [Recall]
Какой тип исключения обычно видит `catch` после `await faultedTask`?

---

### 2. [Recall]
`task.Result` на faulted task — какой тип throw?

---

### 3. [Understanding]
Почему `await` unwraps, а `.Result` wraps?

---

### 4. [Understanding]
`await Task.WhenAll(t1,t2)` — оба fault. Сколько catch blocks сработает и сколько exceptions увидишь в одном catch?

---

### 5. [Application]
Как получить **все** exceptions после `WhenAll` failure?

---

### 6. [Application]
`try { await Save(); } catch { return; }` — проблема?

---

### 7. [Analysis]
`throw ex;` vs `throw;` после catch в async method?

---

### 8. [Analysis]
`async void ButtonClick()` без try/catch — что при exception?

---

### 9. [Understanding]
`finally` с `await CleanupAsync()` после faulted `await` в try — выполнится?

---

### 10. [Recall]
Что такое `UnobservedTaskException`?

---

### 11. [Application]
Exception filter `when` работает с async/await?

---

### 12. [Understanding]
Middleware ASP.NET Core видит exception из `async Task` action когда?

---

### 13. [Analysis]
Library code делает `.Wait()` на async method — два класса проблем?

---

### 14. [Application]
`ExceptionDispatchInfo` — зачем?

---

### 15. [Understanding]
`TaskCanceledException` vs `OperationCanceledException` при cancel token — что catch'ить в ASP.NET?

---

## Self-Check

| # | Your answer | Confidence 1–5 |

**Scoring:** ≥12/15 (80%).

# Exercises: Async All the Way

> Complete after passing `4.test-yourself.md`.

## Easy

### E1. Find the block
**Goal:** Code review.

**Steps:** In snippet with controller `.Result`, middleware `.Wait()`, repository sync EF — list each violation.

### E2. Rewrite signature
**Goal:** Cascade async.

**Steps:** Given sync `IOrderRepository.GetById` used by async service mismatch — sketch interface change + controller update.

---

## Medium

### M1. Middleware fix
**Goal:** InvokeAsync.

**Steps:** Rewrite sync middleware using `.Wait()` to proper `InvokeAsync` with await.

### M2. Legacy bridge design
**Goal:** No .Result on request path.

**Steps:** Design channel + hosted service for sync-only `Print()` SDK called from HTTP POST — sequence diagram or bullets.

---

## Hard (stretch)

### H1. Load test hypothesis
**Goal:** Production mindset.

**Steps:** Write 5-step plan to prove thread pool starvation from `.Result` in staging (metrics, tools, fix verify).

---

## Hints

<details>
<summary>Hint M1</summary>
`public async Task InvokeAsync(HttpContext ctx, RequestDelegate next) { ... await next(ctx); }`
</details>

# NuGet — packages, transitive deps, CPM

> Roadmap: `1.5.16` · Node: `1.5` — C#: .NET Runtime и память · Depth: практика

## Learning Objectives

- **PackageReference**, **dotnet restore**, **project.assets.json**.
- **Direct vs transitive** dependencies.
- **Directory.Packages.props** — Central Package Management.
- Команды: **list package**, **nuget why**, **vulnerable**.
- **NU1605**, pinning, lock files.

---

## Why This Matters

Production apps — граф NuGet packages. Transitive vulnerability или downgrade ломает build/runtime. CPM — одна версия на monorepo. Дальше — **`1.6.1` SQL**.

---

## Core Concepts

### PackageReference

```xml
<PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="9.0.2" />
```

**dotnet restore** → **project.assets.json**, global cache.

### Transitive

```bash
dotnet list package --include-transitive
dotnet nuget why App.csproj Newtonsoft.Json
```

### CPM

**Directory.Packages.props:**

```xml
<PropertyGroup>
  <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
</PropertyGroup>
<ItemGroup>
  <PackageVersion Include="Serilog.AspNetCore" Version="8.0.3" />
</ItemGroup>
```

**csproj:** `<PackageReference Include="Serilog.AspNetCore" />` без Version.

### Security

```bash
dotnet list package --vulnerable
```

Lock file: **RestorePackagesWithLockFile**.

---

## Common Mistakes

Floating `*` в CI. Ignore NU1605. Version drift без CPM.

---

## Key Takeaways

- Restore = full graph.
- CPM для monorepo.
- Audit transitive + vulnerable.
- Up Next: **1.6.1** SQL.

---

## Further Reading

- [Central Package Management](https://learn.microsoft.com/en-us/nuget/consume-packages/central-package-management)

---

## Up Next

**`1.6.1`** — SQL fundamentals.

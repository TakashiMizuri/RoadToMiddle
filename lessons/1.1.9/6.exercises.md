# Exercises: Control flow — if/else, switch

> Complete after reading `1.lection-eng.md`, `2.lection-ru.md` and passing `4.test-yourself.md`. Ask AI for hints, not full solutions.

## Easy

### E1. Grade calculator
**Goal:** Practice `if/else if/else` chain.

**Steps:**
1. Create console app `ControlFlow.Grades`.
2. Read integer `score` from console (assume valid 0–100 for now).
3. Print letter grade: A ≥90, B ≥80, C ≥70, D ≥60, else F.

**Success criteria:** Correct boundary behavior; you can explain why order of checks matters.

---

### E2. Weekend detector with classic switch
**Goal:** Use `switch` on `DayOfWeek`.

**Steps:**
1. Use `DateTime.Today.DayOfWeek` or hard-code a day for testing.
2. Classic `switch`: weekend vs weekday message.
3. Include `default` arm.

**Success criteria:** Every case ends with `break`; no duplicate logic without `goto case`.

---

## Medium

### M1. Shipping cost with switch expression
**Goal:** Return a value from `switch` expression.

**Tasks:**
Given `string country` (`"US"`, `"CA"`, `"EU"`, other), compute `decimal shipping` using switch expression:
- US → 5.99m
- CA → 7.99m
- EU → 12.99m
- _ → 19.99m

Print result. Use **`m` suffix** for decimals.

**Success criteria:** Single assignment from switch expression; handles unknown country.

---

### M2. Safe integer input
**Goal:** Combine `TryParse`, guards, and branching.

**Tasks:**
1. Prompt for age.
2. If parse fails → message + early exit.
3. If age < 0 or > 120 → warning branch.
4. Else classify Minor (<18) vs Adult.

**Success criteria:** No unhandled `FormatException`; uses short-circuit null-safe patterns if using nullable strings.

---

### M3. Type describer with patterns
**Goal:** Pattern matching on `object`.

**Tasks:**
Create `object value` (test with `int`, `string`, `null`, `bool`).
Use **switch expression** to produce human-readable description (include length for strings).

**Success criteria:** All test values handled; `_` arm present.

---

## Hard (stretch)

### H1. Menu-driven mini app
**Goal:** Integrate loop preview with switch (full loops in `1.1.10`).

**Tasks:**
Show menu: 1=Add, 2=Subtract, 3=Exit. Read choice; **switch** on choice. For 1–2 read two numbers and print result; 3 sets exit flag. Repeat until exit.

**Success criteria:** Switch handles invalid option via `default`; clean exit without `Environment.Exit` unless you justify it.

---

## Submission checklist

- [ ] Projects compile with `dotnet run`
- [ ] No magic numbers without named constants where clarity helps
- [ ] You can verbally explain classic vs expression switch for one exercise

## Hints (read only if stuck)

<details>
<summary>Hint E1</summary>
Start from highest threshold (90) downward — same lesson as lecture example.
</details>

<details>
<summary>Hint M1</summary>
`decimal fee = country switch { "US" => 5.99m, ... };`
</details>

<details>
<summary>Hint H1</summary>
Use `bool running = true; while (running) { ... switch { case 3: running = false; break; } }`
</details>

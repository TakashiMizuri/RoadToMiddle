# Exercises: CDN

> Complete after lectures and passing `4.test-yourself.md`.

## Easy

### E1. Classify assets
**Goal:** Decide CDN suitability.

| Asset | CDN long TTL? | Why |
|-------|---------------|-----|
| `logo.[hash].png` | | |
| `index.html` | | |
| `GET /api/products` JSON | | |
| `Roboto.woff2` | | |

**Success criteria:** Four rows with yes/no and brief reason.

---

## Medium

### M1. Deploy checklist
**Goal:** Production workflow.

Write 5-step checklist for deploying a new Vite React build to S3 + CloudFront minimizing stale UI risk.

**Success criteria:** Mentions upload, index.html TTL/invalidate, hashed assets.

---

## Hard (stretch)

### H1. Debug stale bundle
**Goal:** Troubleshooting.

Users report old UI after deploy. Hashed JS files on S3 are new. List diagnostic steps in order (curl headers, which URL to check, CDN vs browser cache).

**Success criteria:** Checks index.html cache first; mentions curl -I.

---

## Hints

<details>
<summary>Hint E1 index.html</summary>
Short TTL or no-cache — it points to hashed bundles.
</details>

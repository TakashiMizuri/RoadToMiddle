# Exercises: LINQ SelectMany

## Easy

### E1. Flatten lines
**Tasks:** `TotalQty(all orders)` via **`SelectMany`** + **`Sum`**.

### E2. Fix nested type
**Tasks:** Change **`Select`** to **`SelectMany`** so method returns **`IEnumerable<Line>`** not nested.

## Medium

### M1. Left join with SelectMany
**Tasks:** Customers + optional orders flat list using **`GroupJoin` + SelectMany + DefaultIfEmpty`**.

### M2. Query syntax
**Tasks:** Rewrite M1 with double **`from`**.

## Hard

### H1. Paginated flatten
**Tasks:** **`IEnumerable<Page<T>>`** → lazy **`IEnumerable<T>`** without loading all pages into one **`List`** first.

## Hints

<details><summary>M1</summary>DefaultIfEmpty before SelectMany inner selector.</details>

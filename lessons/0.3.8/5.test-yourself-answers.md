# Answers: HTTPS и TLS

## Scoring

- **90–100%:** отлично — переходи к `0.3.9`
- **80–89%:** pass — повтори weak questions
- **<80%:** вернись к lections Core + Under the Hood

---

## Answers

### 1. [Recall]
**Answer:** **TLS** (Transport Layer Security).

**Why:** HTTPS = HTTP over TLS over TCP.

---

### 2. [Recall]
**Answer:** **443**.

---

### 3. [Recall]
**Answer:** **Chain of trust** (signature to trusted CA) и **hostname match** — `api.example.com` в **SAN**/CN certificate.

**Why:** Authentication of server identity for this URL.

---

### 4. [Understanding]
**Answer:** **Asymmetric** crypto слишком медленный для every byte; **symmetric** (AES-GCM) fast для bulk traffic; session keys derived once per handshake.

**Why:** Hybrid encryption standard in TLS.

---

### 5. [Understanding]
**Answer:** Evil site может иметь **valid HTTPS cert для своего domain** — TLS encrypts channel to **attacker's server**, not your bank. User must verify **URL/domain**, not только padlock icon.

**Why:** TLS authenticates **connected peer**, not user intent.

---

### 6. [Understanding]
**Answer:** **Нет** (typically) — hop nginx→Kestrel часто **plain HTTP на localhost/private network**. Client↔nginx encrypted. Trust internal network or re-encrypt (mTLS) if policy requires.

**Why:** TLS termination pattern splits encryption boundaries.

---

### 7. [Application]
**Answer:** **Certificate expired** — past Not After. Fix: **renew** cert (ACME/Let's Encrypt), deploy full chain, restart proxy/Kestrel.

---

### 8. [Application]
**Answer:** **Нет** — `Secure` cookies **not sent** over cleartext HTTP.

**Why:** Attribute restricts cookie to secure schemes.

---

### 9. [Application]
**Answer:** **`X-Forwarded-Proto: https`** (and `UseForwardedHeaders` in ASP.NET) so app knows external scheme.

**Why:** App sees HTTP from proxy without forwarded metadata.

---

### 10. [Analysis]
**Answer:** **B** неверно — HTTP semantics **unchanged**; only transport encrypted.

---

### 11. [Analysis]
**Answer:** Browser **warning/block** → users abandon; no public **chain of trust** → MITM possible if user clicks through exception; no accountability like public CA DV.

---

### 12. [Understanding]
**Answer:** Browser **automatically uses HTTPS** for domain (max-age), reducing **sslstrip/downgrade** to HTTP on future visits.

**Why:** HSTS policy cached by user agent.

---

### 13. [Analysis]
**Answer:** URLs logged in **server/proxy logs**, **Referer** headers, browser history — HTTPS не скрывает URL from endpoints that see it. Query strings often **cached** accidentally. Passwords belong in body POST + proper auth, not query.

---

## Model answers for practical

```bash
openssl s_client -connect host:443 -servername host
curl -vI https://host
```

---

## If you failed

- Handshake purpose → Core Concepts TLS Handshake.
- Proxy termination → Under the Hood + Example 3.
- Secure cookies → link `0.3.9`.

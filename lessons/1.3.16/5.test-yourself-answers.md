# Answers: IDisposable and using

## Scoring

- 90–100%: отлично
- 80–89%: pass
- <80%: перечитай Core + Under the Hood

---

## Answers

### 1. [Recall]
**Answer:** GC не освобождает **unmanaged handles** promptly; `Dispose` — deterministic release файлов, sockets, DB connections.
**Why:** Native resources вне mark-sweep модели.

### 2. [Recall]
**Answer:** Убирает объект из **finalization queue** — finalizer не вызовется после explicit dispose.
**Why:** Avoid redundant/slower finalization path.

### 3. [Recall]
**Answer:** `await using` вызывает **`DisposeAsync`** (async finally); `using` — sync **`Dispose`**.
**Why:** Async I/O cleanup without blocking.

### 4. [Understanding]
**Answer:** Non-deterministic timing, extra GC cost, no ordering — under load handles exhausted до finalization.
**Why:** Production needs prompt release.

### 5. [Understanding]
**Answer:** **`b` first**, then **`a`** (reverse order of declaration).
**Why:** C# using declaration spec.

### 6. [Understanding]
**Answer:** Multiple dispose calls возможны (finally + user); второй вызов не должен throw или double-free.
**Why:** Robustness + composition.

### 7. [Application]
**Answer:** **Да** — `finally` always runs.
**Why:** using lowering.

### 8. [Application]
**Answer:** **DI container** at end of HTTP request scope disposes scoped services.
**Why:** ASP.NET Core scope lifetime.

### 9. [Analysis]
**Answer:** Нет **idempotent guard** — double dispose может throw; нет **SuppressFinalize** если был finalizer; assigning `_stream` on readonly field may not compile.
**Common mistake:** Minimal Dispose without pattern.

### 10. [Analysis]
**Answer:** **Нет** — pool exhaustion до GC; always explicit dispose/`using`.
**Why:** Finalization delay.

### 11. [Application]
**Answer:** Когда cleanup делает **async I/O** (flush, close connection async) — `IAsyncDisposable`.
**Why:** Avoid sync-over-async block in Dispose.

### 12. [Recall]
**Answer:** Managed: `List<int>`. Unmanaged: socket handle, file descriptor (often via wrapper).
**Why:** GC tracks managed heap objects.

### 13. [Understanding]
**Answer:** BCL `FileStream.Dispose` **idempotent** — второй вызов safe (no-op internally).
**Why:** Framework design.

### 14. [Application]
**Answer:**
```csharp
public void Dispose()
{
    if (_disposed) return;
    // cleanup
    _disposed = true;
    GC.SuppressFinalize(this);
}
```
**Why:** Standard guard.

### 15. [Understanding]
**Answer:** Reachable для GC → помечен для finalization → **survives** collection → promoted → finalized → then collected — **минимум один extra cycle**.
**Why:** Finalization queue mechanics.

---

## If you failed

- GC vs handles → Why This Matters
- using/finally → Core «using Statement»
- Finalizers → Core «Finalizers» + Q15
- async → IAsyncDisposable section

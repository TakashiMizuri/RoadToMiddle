# Self-Test: Async all the way

> ⛔ Не открывай `5.test-yourself-answers.md` до завершения.
> Время: ~15 мин · Проходной балл: **80%**

---

## Questions

### 1. [Recall]
Определение **sync-over-async** одним предложением.

---

### 2. [Recall]
Почему ASP.NET Core без SyncContext всё равно страдает от `.Result`?

---

### 3. [Understanding]
Чем **async-over-sync** через `Task.Run(File.ReadAllBytes)` отличается от `ReadAllBytesAsync`?

---

### 4. [Understanding]
Что такое **interface cascade** при refactor to async?

---

### 5. [Application]
Refactor: `public IActionResult Get() => Ok(_svc.GetAsync().Result);` — новая сигнатура?

---

### 6. [Application]
Middleware sync `Invoke` вызывает `_next(context).Wait()`. Проблема?

---

### 7. [Analysis]
Команда: «Task.Run оборачивает все DB вызовы — теперь async». Аргумент против?

---

### 8. [Analysis]
`.GetAwaiter().GetResult()` vs `.Result` в ASP.NET — разница для pool?

---

### 9. [Understanding]
Когда `Task.Run` **уместен** в ASP.NET?

---

### 10. [Recall]
Sync-over-async + UI `SynchronizationContext` — типичный исход?

---

### 11. [Application]
Legacy sync-only SDK в HTTP handler. Два architecture option?

---

### 12. [Understanding]
Почему ctor не должен `.Result` на async init?

---

### 13. [Analysis]
EF: `return _context.Orders.Where(...);` без ToListAsync в async action — риск?

---

### 14. [Application]
Load test: thread pool queue length растёт. Первые три места grep в codebase?

---

### 15. [Understanding]
`ConfigureAwait(false)` заменяет async all the way?

---

## Self-Check

| # | Your answer | Confidence 1–5 |

**Scoring:** ≥12/15 (80%).

# Self-Test: SemaphoreSlim for throttling

> ⛔ Не открывай `5.test-yourself-answers.md` до завершения.
> Время: ~15 мин · Проходной балл: **80%**

---

## Questions

### 1. [Recall]
Что делает `SemaphoreSlim.WaitAsync()` когда available permits = 0?

---

### 2. [Recall]
Почему `Release()` обычно в `finally`?

---

### 3. [Understanding]
Чем throttling через `SemaphoreSlim` отличается от bounded `Channel`?

---

### 4. [Understanding]
Почему `lock (obj) { await Foo(); }` опасен?

---

### 5. [Application]
Max 3 concurrent HTTP calls к 100 URL. Опиши pattern (без полного кода).

---

### 6. [Application]
`WaitAsync(ct)` бросил `OperationCanceledException`. Нужен ли `Release()`?

---

### 7. [Analysis]
После нескольких часов работы все запросы висят на `WaitAsync`. Вероятная причина?

---

### 8. [Analysis]
`new SemaphoreSlim(1000)` при SQL pool size 100. Проблема?

---

### 9. [Recall]
Что бросает double `Release()`?

---

### 10. [Understanding]
`Parallel.ForEachAsync` с `MaxDegreeOfParallelism = 5` vs manual semaphore — когда semaphore всё ещё нужен?

---

### 11. [Application]
Напиши сигнатуру helper `RunThrottledAsync(IEnumerable<Func<Task>> jobs, int maxConcurrency, CancellationToken ct)`.

---

### 12. [Understanding]
Semaphore ограничивает RPS (requests per second)?

---

### 13. [Analysis]
Создавать `new SemaphoreSlim(10)` внутри каждого controller action?

---

### 14. [Application]
Как проверить leak permits через `CurrentCount`?

---

### 15. [Understanding]
Binary semaphore: `new SemaphoreSlim(1, 1)` vs `lock` для короткой sync секции?

---

## Self-Check

| # | Your answer | Confidence 1–5 |

**Scoring:** ≥12/15 (80%).

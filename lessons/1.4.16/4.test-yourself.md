# Self-Test: IAsyncEnumerable — async streams

> ⛔ Не открывай `5.test-yourself-answers.md` до завершения.
> Время: ~15 мин · Проходной балл: **80%**

---

## Questions

### 1. [Recall]
Какой синтаксис consumer'а используется для `IAsyncEnumerable<T>`?

---

### 2. [Recall]
Какой attribute связывает `CancellationToken` из `await foreach` с параметром async iterator?

---

### 3. [Understanding]
Чем `MoveNextAsync()` принципиально отличается от `MoveNext()` в контексте I/O-bound источника?

---

### 4. [Understanding]
Что такое **implicit backpressure** в pull-based async stream?

---

### 5. [Understanding]
Почему `IEnumerable` с `Task.Result` внутри iterator — anti-pattern для paginated DB read?

---

### 6. [Application]
Напиши сигнатуру метода, который stream'ит строки из файла построчно с поддержкой cancellation.

---

### 7. [Application]
Consumer должен остановиться при `CancellationToken`. Какой фрагмент `await foreach` корректен?

- A) `await foreach (var x in stream)`
- B) `await foreach (var x in stream.WithCancellation(ct))`
- C) `foreach (var x in stream)`

---

### 8. [Analysis]
Метод возвращает `IAsyncEnumerable<Order>`. Вызывающий код делает `var list = orders.ToList()` сразу. Какая проблема на больших данных?

---

### 9. [Analysis]
Когда **Channels** предпочтительнее `IAsyncEnumerable`?

---

### 10. [Recall]
Что делает `yield break` в `async IAsyncEnumerable`?

---

### 11. [Understanding]
Зачем `await foreach` генерирует вызов `DisposeAsync`?

---

### 12. [Application]
EF query: `context.Orders.Where(...).AsAsyncEnumerable()`. Consumer — `await foreach`. Что по памяти по сравнению с `ToListAsync()`?

---

### 13. [Understanding]
Нужен ли NuGet **System.Linq.Async** для `Where` на `IAsyncEnumerable`?

---

### 14. [Analysis]
Два последовательных `await foreach` по одному `IAsyncEnumerable` из EF — что может произойти?

---

### 15. [Application]
Minimal API stream'ит NDJSON. Назови два аспекта production (не syntax), которые нужно проверить.

---

## Self-Check

| # | Your answer | Confidence 1–5 |
|---|-------------|----------------|
| 1 | | |
| 2 | | |
| ... | | |

**Scoring:** 15 questions · need **≥12 correct (80%)** to pass.
